binary_sensor:
  - platform: mqtt
    state_topic: "r433/moon/switch"
    name: r433_moon_state
    payload_on: "on"
    payload_off: "off"
    device_class: light

  # - name: r433_moon_state
  #   platform: command_line
  #   command: "curl -X GET --silent --header 'Accept: application/json' 'http://rpi-433rc:5000/devices/moon'"
  #   device_class: light
  #   value_template: '{{value_json.state}}'
  #   payload_on: 'on'
  #   payload_off: 'off'
  #   scan_interval: 5

switch:
  - platform: template
    switches:
      r433_moon:
        value_template: "{{ is_state('binary_sensor.r433_moon_state', 'on') }}"
        turn_on:
          service: rest_command.r433_switch_on
          data:
            device_name: moon
        turn_off:
          service: rest_command.r433_switch_off
          data:
            device_name: moon
        icon_template: mdi:weather-night
